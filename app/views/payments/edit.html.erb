<% provide :title, "Cambiar Metodo de Pago" %>
<% content_for :conekta_scripts do %>
  <script type="text/javascript" src="https://conektaapi.s3.amazonaws.com/v0.3.2/js/conekta.js"></script>
  <script type="text/javascript">
  // Conekta Public Key
  Conekta.setPublishableKey('<%= Rails.application.secrets.conekta_public %>');

  var conektaSuccessResponseHandler = function(token) {
    var $form = $("#card-form");
    //Inserta el token_id en la forma para que se envíe al servidor
    $form.append($("<input type=\"hidden\" name=\"conektaTokenId\" />").val(token.id));
    $form.get(0).submit(); //Hace submit
  };
  var conektaErrorResponseHandler = function(response) {
    var $form = $("#card-form");
    $form.find(".card-errors").text(response.message_to_purchaser);
    $form.find("button").prop("disabled", false);
  };

  //jQuery para que genere el token después de dar click en submit
  $(function () {
    $("#card-form").submit(function(event) {
      var $form = $(this);
      // Previene hacer submit más de una vez
      $form.find("button").prop("disabled", true);
      Conekta.token.create($form, conektaSuccessResponseHandler, conektaErrorResponseHandler);
      return false;
    });
  });

</script>

<% end %>

<div id="current-card">
  <div class="col-md-1 col-sm-1 col-xs-1"><br></div>
  <div class="col-md-10 col-sm-10 col-xs-10">
      <%= form_tag(payment_path(@payment), id: 'card-form', method: "patch", class: "form-inline") do %>
  <span class="card-errors"></span>
  <br>
  <div class="row ">
    <div class="col-md-2"><br></div>
    <div class="col-md-8 target">
      <div class="form-group">
      <h3>Realizar compra<%= image_tag(@img_card, class: "img-responsive card-img", id: "chip") %></h3>

      <br>
      <%= image_tag("chip.png", class: "img-responsive chip") %>
      <br>
      <label>
        <span>Número de tarjeta de crédito</span><br>
        <input class="form-control field-pay" type="text" size="20" value="**** **** **** <%= @payment_sources.last4 %>" disabled/>
      </label>
    </div>
    <br>
    <div class="form-group">
      <label class="fecha">
        <span>Fecha de expiración (MM/AAAA)</span><br>
        <input class="form-control field-pay date" type="text" size="2" value="<%= @payment_sources.exp_month %>" disabled/>

      <span>/</span>
      <input class="form-control field-pay date" type="text" size="4" value="<%= @payment_sources.exp_year %>" disabled/>
      </label>
    </div>
    <div class="form-group">
      <label>
        <span>CVC</span>
        <input class="form-control field-pay" type="text" size="4" value="***" disabled/>
      </label>
    </div>
    <br>

    </div>
    <div class="col-md-2"></div>
  </div>
<% end %>

<button class="btn btn-default change-payment send-button center-block">Cambiar Metodo de Pago</button>


  </div>
  <div class="col-md-1 col-sm-1 col-xs-1"></div>
</div>

<div id="edit-card" style="display: none">
  <div class="col-md-1 col-sm-1 col-xs-1"><br></div>
  <div class="col-md-10 col-sm-10 col-xs-10">
      <%= form_tag(payment_path(@payment), id: 'card-form', method: "patch", class: "form-inline") do %>
  <span class="card-errors"></span>
  <div class="form-group">
    <label>
      <span>Nombre del tarjetahabiente</span>
      <input id="name" class="form-control field-pay" type="text" size="20" name="name" data-conekta="card[name]" required/>
    </label>
  </div>
  <br>
  <br>
  <div class="form-group">
    <label>
      <span>Telefono</span>
      <input class="form-control field-pay" type="text" size="20" name="phone" data-conekta="card[phone]" required/>
      <%= image_tag("master.png", class: "img-responsive card-img") %>
      <%= image_tag("visa.png", class: "img-responsive card-img") %>
      <%= image_tag("american.png", class: "img-responsive card-img") %>

    </label>
  </div>
  <br>
  <div class="row ">
    <div class="col-md-2"><br></div>
    <div class="col-md-8 target">
      <div class="form-group">
      <br>
      <%= image_tag("chip.png", class: "img-responsive chip") %>
      <br>
      <label>
        <span>Número de tarjeta de crédito</span><br>
        <input class="form-control field-pay" type="text" size="20" data-conekta="card[number]" required/>
      </label>
    </div>
    <br>
    <div class="form-group">
      <label class="fecha">
        <span>Fecha de expiración (MM/AAAA)</span><br>
        <input class="form-control field-pay date" type="text" size="2" data-conekta="card[exp_month]" required/>

      <span>/</span>
      <input class="form-control field-pay date" type="text" size="4" data-conekta="card[exp_year]" required/>
      </label>
    </div>
    <div class="form-group">
      <label>
        <span>CVC</span>
        <input class="form-control field-pay" type="text" size="4" data-conekta="card[cvc]" required/>
      </label>
    </div>
    <br>

    <button type="submit" class="pay-now">Cambiar</button>
    </div>
    <div class="col-md-2"></div>
  </div>
<% end %>


  </div>
  <div class="col-md-1 col-sm-1 col-xs-1"></div>
</div>
