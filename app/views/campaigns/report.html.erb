<%= provide :title, t("views.campagins.report.title") %>

<div class="row">
  <div class="col-md-4 col-md-offset-1">
    <%= form_for :filter, url: report_campaign_path, method: :post, html: { id: 'filter-form' } do |f| %>
      <div class="form-group">
        <h4><%= t("views.campagins.report.period") %></h4>
        <%= f.select :nps_date, Campaign.nps_dates.map { |key, value| [value, key] }, { selected: @active_filter }, class: 'report-select' %>
      </div>

      <h4><%= t("views.campagins.report.period") %></h4>
      <div class="form-group" id="topics-checkboxes">
        <% @campaign.tmp_topics.each do |topic| %>
          <%= label_tag topic, Campaign.translated_topics[topic], class: 'control-label' %>
          <% if @selected_topics.include? topic %>
            <%= check_box_tag "topics[]", topic, { checked: true } %>
          <% else %>
            <%= check_box_tag "topics[]", topic %>
          <% end %>
        <% end %>
      </div>

      <!-- DO NOT REMOVE DATEPICKER RANGE -->
      <!-- <div class="form-group">
        <div class="input-daterange">
          <div class="row">
            <div class="col-md-6"><%#= f.text_field :start_date, class: 'form-control', placeholder: 'Fecha >= a' %></div>
            <div class="col-md-6"><%#= f.text_field :end_date, class: 'form-control', placeholder: 'Fecha <= a' %></div>
          </div>
        </div>
      </div> -->

      <div class="form-group">
        <%= f.submit t("views.campagins.report.collapse_button"), class: 'btn btn-default send-button button-100' %>
      </div>
    <% end %>
  </div>

  <div class="col-md-2 col-md-offset-2">
    <%= link_to t("views.campagins.report.reset_button"), report_campaign_path, class: 'btn btn-default send-button' %>
  </div>

  <div class="col-md-2">
    <% date = @active_filter || '1' %>
    <% feedback_type = @feedback_type || 'none' %>
    <%= link_to t("views.campagins.report.download_button"), report_campaign_path(format: :csv, nps_date: date, feedback_type: feedback_type), class: 'btn btn-default send-button', id: 'download-report' %>
    <h3><%= @feedback_type %></h3>
  </div>
</div>

<div class="row">
  <div class="col-md-9 col-md-offset-1">
    <div id="graph_report"></div>
  </div>

  <%= javascript_tag do %>
    <%=
      graph_generate(
        'graph_report',
        data: {
          x: 'x',
          columns: [
            ['x'] + @nps.dates,
            ['data5'] + @nps.nps,
            ['data6'] + @nps.detractors,
            ['data7'] + @nps.passives,
            ['data8'] + @nps.promoters 
          ],
          names: {              
              data5: t("views.campaigns.dashboard.graph.column5"),
              data6: t("views.campaigns.dashboard.graph.column6"),
              data7: t("views.campaigns.dashboard.graph.column7"),
              data8: t("views.campaigns.dashboard.graph.column8")
          axes: {
            'NPS' => 'y'
          }
        },
        legend: {
          show: false
        },
        axis: {
          x: {
            type: 'category'
          },
          y: {
            max: 95,
            min: -95
          }
        }
      )
    %>
  <% end %>
</div>

<div id="contacts-feedback"><%= render partial: 'feedback' %></div>
